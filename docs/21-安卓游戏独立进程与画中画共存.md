# 19. 安卓:游戏独立进程(:game)+ 启动器画中画共存(架构落地)

> 本轮**只打通架构**:两个进程、通道、画中画共存、结束游戏只结束游戏。
> **没有接游戏主类**(那一步等 JRE 包到位);代码里没有任何写死的 JRE 路径、不下载任何东西。
> 用户 2026-09-21 拍板原话:"那我选 B。画中画我们实践过。" —— B 就是本文这一套摆法。
>
> 本文同时是"游戏运行器"**边界接口**的定义(§3)与**将来拆成两个 APK 的接口草案**(§7)。

## 0. 结论(一句话)

游戏的 Activity 声明在 `android:process=":game"` 里(自己一个进程、自己一个窗口、自己的
SurfaceView);两个进程之间只有一条**本地 socket 通道**(主进程监听、游戏进程 connect);
拉起顺序是"**游戏先起(启动器还在前台)-> 游戏窗口就绪后把启动器拉回前台进画中画**"
(§2 说明为什么不能反过来);"结束游戏" = 主进程在通道上发 `END`,游戏进程有序收尾
(flush 日志 + 写退出记录)后**只结束 :game 这一个进程**;真机读数在 §5。

## 1. 摆法

    ┌────────────────────────────┐   本地 socket(AF_UNIX 抽象名)   ┌──────────────────────────┐
    │ 主进程 com.silentstudio.sxcl │ ──────────────────────────────▶ │ :game 进程                │
    │  SxclActivity(Qt / 画中画)   │  主->游戏:END                    │  GameActivity(SurfaceView)│
    │  GameHost(界面适配层)        │  游戏->主:HELLO/LOG/STATE/PING/   │  sxcl_game.c(通道 + 日志)│
    │  GameHost §B(运行器通道)     │            EXIT/SIG              │  传输层(sxcl_game.c ①)   │
    └────────────────────────────┘                                  │  sxcl_jre_bootstrap.c    │
                 ▲                                                   │  └─ 核心库 sxcl/jvm.h     │
                 │ 画中画(pinned task,浮在游戏之上)                    └──────────────────────────┘
            游戏全屏(task affinity = ...game)

## 2. 拉起顺序:**被系统规则逼出来的**,不是偏好

### 2.1 死路:"启动器先进画中画,再去起游戏"

直觉上应该"先让启动器进画中画(保住可见),再拉游戏"。真机(Android 15/16 级 ROM,
targetSdk 34)实测**被系统直接拒绝**,而且 `startActivity` **不抛异常**、静默失败 ——
只有看系统日志才知道。原文(192.168.200.164:5555,2026-09-21 20:28:06):

    E ActivityTaskManager: Background activity launch blocked! goo.gle/android-bal
      [callingPackage: com.silentstudio.sxcl; callingPackageTargetSdk: 34; callingUid: 10170;
       callingPid: 7724; appSwitchState: 2;
       callingUidHasVisibleActivity: true;            <- 我们确实有可见活动……
       callingUidHasVisibleNotPinnedActivity: false;  <- ……但它是 pinned(画中画),不算数
       callingUidHasNonAppVisibleWindow: false;
       callingUidProcState: TOP; intent: Intent { flg=0x10000000 cmp=com.silentstudio.sxcl/.GameActivity }]
    E ActivityTaskManager: Abort background activity starts from 10170
    I ActivityTaskManager: START u0 {cmp=com.silentstudio.sxcl/.GameActivity} from uid 10170
      (BAL_BLOCK) result code=102

读法:**画中画里的活动发起新活动 = 后台活动启动(BAL)**,系统一律拒(result code=102),
而我们的代码只看到"startActivity 返回了",于是 `:game` 进程根本没起来。
(能解开这条的钥匙只有"非 pinned 的可见活动"或"非应用可见窗口"—— 后者就是
SYSTEM_ALERT_WINDOW 悬浮窗,需要用户授权,不做默认路径。)

### 2.2 现在这一套:游戏先起,再由游戏把启动器请回画中画

1. `GameHost.requestStart()`:`openChannel(spec)`(§B)先在**主进程**里把本地 socket 监听
   建好、会话目录建好(游戏出生时监听必须已经在);
2. 启动器**此刻还是前台(可见、非 pinned)** -> `startActivity(GameActivity)` —— **允许**;
3. 游戏窗口就绪 -> **握手**(避免"已经在画中画里还被再拉出来一次",那会把游戏窗口打断):
   运行器在通道上发 `PIP?`,**主进程**回答 `PIP need=0|1`(`need = !isInFloating()` ——
   画中画状态只有主进程知道,静态字段各进程各一份)。`need=1` 时运行器才(作为前台的一侧)
   发起 `startActivity(SxclActivity, REORDER_TO_FRONT|SINGLE_TOP|NEW_TASK + makeLaunchIntoPip)`
   并带 `sxclRequestPip=1`;系统若不吃 launchIntoPip 也不要紧,启动器回前台后在
   `onNewIntent`(已在前台时没有 onResume)或 `onResume`(被拉回前台时)里调
   `enterFloating()`(**复用最大化键那条画中画实现**,失败重试两次并如实记日志);
4. 结果:游戏全屏在下、启动器画中画在上;游戏进程不因画中画受任何影响(pinned 窗口不构成遮挡,
   游戏活动保持 resumed)。
5. 兜底:20s 内没收到运行器的画中画请求,主进程**如实记一行**"启动器此刻不在可见处",
   不做假动作。
6. 如实说明一处取舍:spec 里的 `launcherPip` 目前运行器**固定会问一次**("要不要进画中画"),
   由主进程按 `need` 决定 —— 也就是说这个开关暂时不会被用来"完全禁止画中画"。

画中画里的出口仍然是那个原生按钮「回到启动器」(挂在窗口 decor 上,零权限,点它 = `exitFloating()`)。

## 3. "游戏运行器"边界接口(为将来拆成独立 APK 留的口子)

### 3.1 输入:`GameHost.GameLaunchSpec`(android/java/com/silentstudio/sxcl/GameHost.java §A 段)

| 字段 | 含义 | 现在怎么用 |
|---|---|---|
| `jreHome` | JRE 根目录 | 交给核心库 -> `-Djava.home` + dlopen libjli |
| `classpath` | 游戏 classpath | -> `-cp` |
| `nativesDir` | natives 目录 | -> `-Djava.library.path`(默认填 APK 的 nativeLibraryDir) |
| `gameDir` | 游戏目录 | -> `-Duser.dir` |
| `jvmArgs` | JVM 参数(空格分隔) | 主类之前的 argv |
| `gameArgs` | 游戏参数 | 主类之后的 argv |
| `mainClass` | 主类 | 本轮**不接**,为空时跑自检参数 |
| `renderer` | 渲染器 | 只进边界与日志:**渲染层本轮没接**(不假装有) |
| `sessionId` / `socketName` / `sessionDir` | 会话与通道 | 名字空间会合点 + 日志/退出记录落盘处 |
| `crashMode` | 崩溃注入(验收用) | 0 关 / 1 abort / 2 SIGTERM / 3 SIGKILL |
| `launcherPip` | 窗口就绪后请启动器进画中画 | 为什么必须由运行器发起见 §2.1 |

字段一律 String/int(不用 Parcelable):换传输介质(Intent extras -> Binder/传 fd)时不用改数据结构。

### 3.2 输出:日志流 / PID / 退出码或信号 / 结束命令

同一份 `GameHost.java` 里切成三段,**缝切在同一个文件里,不另立包/层**:

| 段 | 内容 | 有没有 UI 依赖 |
|---|---|---|
| §A 边界契约 | `GameLaunchSpec` + 协议动词 + 字段读法 + `Listener` 回调 | 无(只有 `Bundle` 当传输介质) |
| §B 运行器通道 | `openChannel/stopChannel/stateLine` + 接受连接/逐行解析/退出码与信号回收/状态机 | **无**:没有 Activity/View/画中画/Intent,连 Context 都没有(会话目录由 spec 给绝对路径) |
| §C 启动器适配 | `attach/requestStart/startGameActivity/onPipChanged/endGame` | 有(Context/Intent/画中画时序),只有这一段有 |

    // §B(零 UI)
    String  openChannel(GameLaunchSpec spec, Listener listener);  // 建会话 + 监听通道
    boolean stopChannel();                                       // 发 END(唯一控制命令)
    String  stateLine();
    // §A 事件
    interface Listener { void onLog(String); void onPid(int, String);
                         void onExit(int code, int signal, String reason, String source);
                         void onState(String); }

### 3.3 协议(行文本 + `key=value`,`GameHost.java` §A 段是唯一定义处)

    A -> B : END | PING?                     (END 是唯一的控制命令)
    B -> A : HELLO pid=N proc=<进程名> session=<id> chan=0|1 java=present|absent
             READY dir=... nativelib=... mainclass=... classpath=... gamedir=... renderer=...
             STATE ...            LOG <一行>            PING pid=N n=<计数>
             EXIT code=N signal=N reason=command|jvm-returned|body-returned
             SIG signal=N code=128+N      (崩溃兜底:先发这条,再 _exit(128+N))
             BYE
    B -> A : PIP?                        (窗口就绪;要不要把启动器拉回前台进画中画?)
    A -> B : PIP need=0|1                (主进程按自己的画中画状态回答)

### 3.4 异常断开(三种情况,都报事实、不猜)

1. **运行器自己崩**(ABRT/BUS/ILL/FPE/TERM/INT,可捕获):崩溃兜底(pipe + 看门线程)在
   `_exit(128+sig)` **之前**把 `SIG signal=N code=128+N` 用裸 `send()`/`write()` 写进通道
   **并且**写进**预先打开**的退出记录文件(`<session>/exit.txt`;崩溃路径不 malloc、不 open、不加锁)。
2. **被 kill -9 / OOM 杀**:信号不可捕获,通道直接 EOF。主进程按"EOF + 没有退出记录 +
   `/proc/<pid>` 消失"报 `killed`,并在日志里明说 **退出码/信号从进程外面不可观测** ——
   我们不是它的父进程,`waitpid`/`pidfd` 都拿不到退出状态,所以**不编数字**。
3. **主进程先没了**:运行器 `send()` 失败只把通道置灰,日志照旧写自己的文件、logcat 照旧,
   **游戏继续跑** —— 通道不是游戏的前提。

日志三条出口:运行器自己的 `<session>/game.log`(flush+fsync)、通道(主进程转 logcat tag `sxcl`)、
logcat tag `sxcl-game`。主进程每次状态变化另落一份 `<session>/host-state.txt`(`run-as` 就能读)。

## 4. 改了哪些文件

| 文件 | 作用 |
|---|---|
| `android/AndroidManifest.xml` | 新增 `GameActivity`:`android:process=":game"`、`exported=false`、`launchMode=singleTask`、`resizeableActivity=true`、`taskAffinity="com.silentstudio.sxcl.game"`(**必须**:singleTask 同 task 会把启动器从栈上清掉)、`configChanges=orientation\|keyboardHidden\|screenSize\|screenLayout\|smallestScreenSize\|density\|uiMode`、全屏无标题栏主题 |
| `android/java/.../GameHost.java` | **重写**。一个文件三段(§3.2):§A 边界契约(`GameLaunchSpec` + 协议 + `Listener`)、§B 运行器通道(**零 UI/零 Context** 的 socket + 状态机 + 退出码/信号回收)、§C 启动器适配(Context/Intent/画中画时序)。对外仍是 `requestStart/endGame/stateLine/isSessionLive` 那几个静态方法 |
| `android/java/.../GameActivity.java` | **新增**。`:game` 进程窗口:自己的 SurfaceView、常亮、全屏;`System.loadLibrary("sxclgame")`;`nativeStart(spec...)` 独立线程;`onResume` 里请启动器进画中画;原生回调 `onGameStopped` -> `finishAndRemoveTask()` + `Process.killProcess(myPid())`(**只结束 :game**) |
| `android/java/.../SxclActivity.java` | `GameHost.attach()`;画中画回调转发;画中画里的原生按钮「回到启动器」;收到 `sxclRequestPip` 后在 `onResume` 里 `enterFloating()`(重试两次);`onNewIntent` 支持 `--es action gamestart/endgame`;boot 文件新增 `gamestart/gamejre/gamemain/gameargs/gamecrash/gamedelay/gamestop` |
| `android/app/sxcl_jre_bootstrap.h/.c` | 安卓侧**薄适配层**:崩溃兜底(信号 -> 看门线程 -> fatal 钩子 -> `_exit(128+sig)`)、日志出口钩子、JVM 收尾;**环境变量与 argv 全部交给核心库 `sxcl/jvm.h`**(收敛,见 §6) |
| `android/app/sxcl_jre_probe.c` | 变成薄壳:只留探针入口,干活的全在适配层 + 核心库 |
| `android/app/sxcl_game.c` | **新增**。`:game` 进程原生层,**一个文件两段**:① 传输层(AF_UNIX 抽象名字 + 按行收发,纯 C、零 UI 依赖,可整段搬走);② 游戏进程逻辑(连通道、日志三条出口、崩溃兜底钩子、命令读取、心跳、有序收尾、JNI 入口 `nativeStart/nativeRequestStop/nativeHasJvm`、验收用崩溃注入) |
| `android/app/sxcl_android_main.cpp` | 安卓入口:解析 boot 的 `gamestart` 等键;`Q_COREAPP_STARTUP_FUNCTION` 里建会话 + 起游戏;`gamestop` 到点由 Qt 侧发 `endGameSession()` |
| `android/app/CMakeLists.txt` | 新增 `sxclgame` 目标(`libsxclgame.so`,链核心库 `sxcl` + log + dl;**不链 Qt**);`sxclui` 与 `sxclgame` 共用 `sxcl_jre_bootstrap.c` |
| `android/scripts/build_apk.ps1` | 编 `sxclui sxclgame`;`libsxclgame.so` 必须进 `libs/<abi>/`,缺了直接 abort |
| `android/scripts/gameproc_accept.ps1` | **新增**。真机验收脚本(RUN A/A2/A3/B/C/D/E),每条结论后面都跟原始命令与原始输出 |

**桌面零影响**:上面每一处都在 `android/` 下;两棵树的 CMake 都不引用 `android/`
(`src/**`、`include/**`、`tests/**`)。

## 5. 结束游戏 = 只结束游戏(逐条)

1. 主进程 `GameHost.endGame()`(§B `stopChannel()`)在通道上写 `END`;
2. 运行器 `chan_reader` 收到 `END` -> `game_request_stop("command")`:
   ① `sxcl_jre_request_shutdown()`(有 JVM 才做事;没有 JVM 返回 0,走 flush 路径)
   ② `game_flush_log()`(fsync + 写一行 `flush ok lines=... bytes=...`)
   ③ `game_exit_record()`(只能写一次:通道 + `exit.txt`)并通知 Java;
3. `GameActivity.onGameStopped()` -> `finishAndRemoveTask()` -> 300ms 后
   `Process.killProcess(Process.myPid())` —— **杀的是 :game 这一个进程**;
4. 主进程侧 `stop()` 起观察线程轮询 `/proc/<pid>`,确认 `:game` 消失(8s 窗口),
   日志写"确认 :game 进程已消失(主进程照常活着)"。

**没有**任何一步用"杀掉整个 App"实现结束游戏:`am force-stop` 只在验收脚本**每轮开始前**
清状态,不参与"结束游戏"这条路径。

## 6. 与核心库的收敛(2026-09-21)

核心库补齐了 `include/sxcl/jvm.h` + `src/services/launch/jvm.c`(`sxcl_jvm_build_env()` /
`sxcl_jvm_build_args()` / `sxcl_jvm_launch()`),**环境变量与 JVM 参数的拼法只许有这一份**,
所以本轮的 `android/app/sxcl_jre_bootstrap.c` 从"自己算 env + 自己拼 argv + 自己 dlopen"
收敛成**薄适配层**:

* 交给核心库:JAVA_HOME / LD_LIBRARY_PATH / TMPDIR / HOME / java.library.path 的计算、
  `-Djava.home`/`-cp`/`-Djava.library.path`/`-Dos.version` 的拼法、`<home>/lib` 与
  `<home>/lib/server` 的 RTLD_GLOBAL 预加载、`dlopen(libjli.so)` + `dlsym(JLI_Launch)` + 调用;
* 留在安卓侧(核心库不该管,而且是进程内起 JVM 特有的):**崩溃兜底**(pipe + 看门线程 +
  fatal 钩子 + `_exit(128+sig)`)、**日志出口钩子**(文件 + 通道 + logcat)、**JVM 收尾**
  (`DestroyJavaVM` 辅助线程,没有 JVM 时是空操作);
* 与 `android/jni/sxcl_jvm_bootstrap.c`(另一位同学的安卓薄封装)的关系:**两者定位不同**——
  那份只跑"起 JVM 打版本与属性"的自检(`sxcl_android_jvm_bootstrap` 不接受主类/classpath),
  本层要跑**游戏主类**(主类 / classpath / JVM 参数 / 游戏参数 / 收尾钩子),
  所以本层直接调核心库的 `sxcl_jvm_launch()`,不再自己算 env/argv,也不再自己 dlopen。
  **没有第二份 env/argv 实现**;唯一还留在两份文件里的是"把 LD_LIBRARY_PATH 交给 linker 私有入口"
  这一步(安卓特有),核心库选择的做法是 RTLD_GLOBAL 预加载 —— 本轮真机没有真 JRE,
  这一步同样走不到,接真包时以核心库那条为准(见 §8 待办)。

## 7. 边界为什么这样切(将来真要拆成两个应用时)

游戏运行器的边界已按**可搬家**的方式切开(§3):输入 jre / classpath / natives / 游戏目录 /
JVM 与游戏参数 / 渲染器;输出 日志流 / 退出码或信号 / PID / "结束游戏"命令;而且不依赖主进程的
UI 对象(GameHost.java 的 §A/§B 两段零 UI、零 Context)。将来若要拆成独立应用,只需把这一块
整体搬走(sxcl_game.c + sxcl_jre_bootstrap.c + GameActivity.java + §A/§B 的契约),再加上
显式 component 的 Intent 拉起与 Binder 上报即可,不需要重写运行器。**当前不开发独立应用版本**
(用户 2026-09-21:"先不开发 B,会莫名增大开发时间。")。
## 8. 还没做 / 下一步

1. **接游戏主类**(等 JRE 包):`--es gamejre <jre home>` 已经把 JRE 路径喂进运行器;
   缺的是"主类 + `-cp` + LWJGL/JNA 的 `-D`"(docs/18 §7.1)。`GameLaunchSpec` 已经把这几个字段备好。
2. **有 JVM 时的收尾**:`DestroyJavaVM` 那条分支本轮真机**没有 JRE,走不到**,只走通 flush 路径。
3. **LD_LIBRARY_PATH 的 linker 私有入口**:收敛后这一步归核心库(RTLD_GLOBAL 预加载);
   接真 JRE 包时要验一次"预加载够不够",不够就把 linker 入口补进核心库(只补一处)。
4. **渲染器**:spec 里的 `renderer` 只进边界与日志;把 `ANativeWindow` 递给未来的渲染层
   (LWJGL 的 Android 后端要它)是下一步。
5. **界面接线**:本轮由 boot 文件 / `am start --es action` 触发;产品化时接到启动页的
   "启动/结束游戏"按钮上(`GameHost.requestStart(spec)` / `GameHost.endGame()` 已经是对外接口)。
6. **后台冻结那一条要进产品行为**(§9.4d):启动器在前台时游戏进程会被系统 cached 冻结,
   这时 `END` 不会立刻生效 —— 界面要么说清楚"回到游戏后才会结束",要么先把游戏任务拉回前台。
7. **§9.6 里那几条"设备离开前没验到"的**:下次上机第一件事就是它们(画中画里起游戏、
   被拒会话的释放、手指点画中画按钮)。
6. `docs/14-源码头注归档.md` 是自动生成的,本轮新增文件还没进那份归档。

## 9. 真机原始输出

设备 192.168.200.164:5555(G6012BS,Android 15/16 级 ROM,targetSdk 34)。
APK:`D:\sxcl_local\out\build\outputs\apk\debug\sxcl-debug.apk`。
下面的原文来自两次构建,逐节标注:
* §9.1–§9.3:`sha256 8C4906604ADB70476202F13839E4666A6102D556E69E3AF9AABAB3494BA37951`(20:44 构建);
* §9.4b–§9.4d:`sha256 88B891A24C202788BC7F7B09EC80D2B0994833D67EE69C651C4D0184AD8AA29A`(21:12 构建,24186249 字节)
  —— 这一版包含"画中画里起游戏先回全屏""listening 会话自动释放""kill -9 后轮询 2s 再下结论"
  "gamecrash 接受 abort|term|kill 字符串"四处修复。
脚本:`android/scripts/gameproc_accept.ps1`(每条结论后面都跟着原始命令与原始输出,
完整记录在 `build\_android\out\gameproc\accept.log`)。

### 9.1 两个进程(验收 ①)

    $ adb -s 192.168.200.164:5555 shell "ps -A | grep sxcl"
    u0_a170      10098   993   17980800 269692 0                   0 S com.silentstudio.sxcl
    u0_a170      10193   993   17200344 147464 0                   0 S com.silentstudio.sxcl:game

    $ adb -s 192.168.200.164:5555 shell "dumpsys activity processes | grep -i sxcl"
      Proc # 1: vis+ 1 F/A/TOP  LCMNFUAT  t: 0 10098:com.silentstudio.sxcl/u0a170 (vis-activity)
      Proc # 0: fg     T/A/TOP  LCMNFUAT  t: 0 10193:com.silentstudio.sxcl:game/u0a170 (top-activity)
      PID #10098: ProcessRecord{... 10098:com.silentstudio.sxcl/u0a170}
      PID #10193: ProcessRecord{... 10193:com.silentstudio.sxcl:game/u0a170}

    $ adb -s 192.168.200.164:5555 logcat -d -s sxcl | findstr "运行器报到"
    20:46:54.4xx 10098 10123 I sxcl : game-session: 运行器报到:HELLO pid=10193
        proc=com.silentstudio.sxcl:game session=s1789994814216 chan=1 java=absent

游戏进程自己那侧的原文(`adb logcat -s sxcl-game`,同一时刻):

    20:46:54.433 10193 10214 I sxcl-game: [sxcl-game] 崩溃兜底就绪(跟踪 SIGABRT/SIGBUS/SIGILL/
        SIGFPE/SIGTERM/SIGINT;SIGSEGV 留给 JVM)
    20:46:54.433 10193 10214 I sxcl-game: === :game 进程原生层启动 ===
    20:46:54.434 10193 10214 I sxcl-game: [sxcl-game] 本进程没有配 JRE(本轮只验架构):
        java=absent 待命中,通道与收尾照常工作
    20:46:54.436 10193 10218 I sxcl-game: [sxcl-game] 启动器画中画握手:主进程回答 need=1

### 9.2 启动器留在可见处(验收 ③ 的"能看到")

    $ adb -s 192.168.200.164:5555 shell "dumpsys activity activities | grep -E 'mode=pinned'"
      * Task{a1efa89 #1337 type=standard A=10170:com.silentstudio.sxcl ... mode=pinned ...}
        bounds=[1475,842][2368,1344]
    $ ... "dumpsys activity activities | grep -E 'mLastReportedPictureInPictureMode|supportsPictureInPicture'"
      mLastReportedPictureInPictureMode=true        FOUND
      supportsPictureInPicture=true                 FOUND
    $ adb -s ... logcat -d -s sxcl | findstr "pip"
    20:41:05.494 10098 10098 I sxcl : pip mode changed: in-pip=true
    20:41:05.495 10098 10098 I sxcl : game-session: 启动器已进入画中画(游戏继续跑,运行器不受任何影响)
    20:41:05.498 10098 10098 I sxcl : pip back-button shown(画中画里的出口;decor children=3)

窗口侧证据(同一个时刻,`dumpsys window windows`):启动器窗口
`Window{e16d849 ... SxclActivity} ... Surface: shown=true mDrawState=HAS_DRAWN` ——
画中画窗口是真的画出来的,不是"标记成 pinned 但没内容"。

**"点回到启动器"这一条的真相(如实写)**:
`adb shell input tap` 在这台 ROM 上**打不进 pinned 窗口** —— 在 PiP 窗口矩形内按阶梯
(dy=20/30/45/60/80,共 6 次)注入点击,logcat 里没有任何 `pip back-button tapped`,
也没有画中画退出。按钮本身在窗口里(`decor children=3` 那行)。因此改用**按钮做的那件事**
来验证意图:`exitFloating()` 就是 `startActivity(SxclActivity, REORDER_TO_FRONT|SINGLE_TOP|
NEW_TASK)`,用同样的意图执行后:

    $ adb -s ... shell am start -n com.silentstudio.sxcl/com.silentstudio.sxcl.SxclActivity
    $ adb -s ... shell "dumpsys window | grep mCurrentFocus"
      mCurrentFocus=Window{... com.silentstudio.sxcl/com.silentstudio.sxcl.SxclActivity}
    $ adb -s ... shell "dumpsys activity activities | grep -c mode=pinned"
      0
    $ adb -s ... shell "pidof com.silentstudio.sxcl:game"
      10193          <- 回到启动器之后游戏进程仍然活着

结论:**"回到启动器 -> 主界面回来 + 游戏仍在跑"这条路径成立**(意图与状态都有原文);
但"手指点在按钮上"这一步**没有在真机上验证过**(adb 注入到不了 pinned 窗口),
不算通过,列进 §8 的待办。

### 9.3 结束游戏:只结束 :game,主进程照常活着(验收 ④)

    $ adb -s ... shell am start -n .../SxclActivity --es action endgame
    20:51:56.426 10098 10098 I sxcl : game-session: 已发出结束命令(END):等运行器有序收尾
        (flush 日志 + 写退出记录)并确认它消失
    20:51:56.429 10098 10215 I sxcl : game-session: 运行器自报退出:EXIT code=0 signal=0 reason=command
    20:51:56.945 10098 10841 I sxcl : game-session: 确认 :game 进程已消失 pid=10193
        (主进程照常活着 pid=10098)
    $ adb -s ... shell "ps -A | grep sxcl"
    u0_a170      10098   993   17750536 261084 0                   0 S com.silentstudio.sxcl

游戏进程侧的收尾原文(`foo` 会话目录里 `game.log` 的末行 + `exit.txt`):

    $ adb -s ... shell "run-as com.silentstudio.sxcl cat files/game/<session>/exit.txt"
    pid=10193
    proc=com.silentstudio.sxcl:game
    session=s1789994814216
    code=0
    signal=0
    reason=command
    ...
    [sxcl-game] flush ok lines=N bytes=M log=/data/user/0/com.silentstudio.sxcl/files/game/<session>/game.log

### 9.4 游戏进程死了,主进程还在,并且报得出信号(验收 ②)

可捕获的信号(SIGTERM,`run-as ... kill -TERM <:game pid>`):

    $ adb -s ... shell "run-as com.silentstudio.sxcl kill -TERM 11856"
    20:56:45.444 11794 11881 I sxcl : game-session: 运行器崩溃兜底回传信号:SIG signal=15 code=143
    20:56:45.487 11794 11881 I sxcl : game-session: 运行器已结束:信号=15 退出码=143 来源=socket
                                       pid-alive=1
    20:56:45.486 11794 11881 I sxcl : game-session: 通道已断开(EOF)
    $ adb -s ... shell "ps -A | grep sxcl"
    u0_a170      11794   993   17701400 275172 0                   0 S com.silentstudio.sxcl
    $ adb -s ... shell "run-as com.silentstudio.sxcl cat files/game/<session>/exit.txt"
    SIG signal=15 code=143

不可捕获的信号(SIGKILL)——**如实报告,不编数字**:

    $ adb -s ... shell "run-as com.silentstudio.sxcl kill -9 12189"
    21:00:20.169 12129 12216 I sxcl : game-session: 通道已断开(EOF)
    21:00:20.169 12129 12216 I sxcl : game-session STATE: ... game-pid=12189 state=disconnected
        connected=0 ... reason=channel-closed(pid 仍在) source=proc-poll
    $ adb -s ... shell "ps -A | grep sxcl"
    u0_a170      12129   993   17718780 272732 0                   0 S com.silentstudio.sxcl

(最后这条就是"必须写明拿不到什么"的例子:SIGKILL 时进程表可能还残留一小会儿,
主进程按 `/proc/<pid>` 下的结论可能落在 disconnected;退出码/信号从进程外面
**本来就不可观测**(我们不是它的父进程,`waitpid`/`pidfd` 都拿不到),
所以这里只会写"没有退出记录",不会编一个 137 出来。已按此调整:EOF 后再轮询 2s 才下结论。)

### 9.4b 最终 APK 上的三种结束方式 + 崩溃注入(全部原文)

最终 APK(`sha256 88B891A24C202788BC7F7B09EC80D2B0994833D67EE69C651C4D0184AD8AA29A`,
24186249 字节,21:12 构建;含"画中画里起游戏先回全屏""卡在 listening 的会话自动释放"
"kill -9 后轮询 2s 再下结论""gamecrash 接受 abort|term|kill 字符串"四处修复):

    ════ 1) --es gamecrash abort(SIGABRT,可捕获)
    game-session: 运行器报到:HELLO pid=... proc=com.silentstudio.sxcl:game chan=1 java=absent
    game-session: 运行器已结束:信号=6 退出码=134 来源=socket pid-alive=1
    $ adb shell ps -A | grep sxcl
    u0_a170  17778  ... S com.silentstudio.sxcl          <- 只剩主进程

    ════ 2) --es gamecrash term(SIGTERM,可捕获)
    [game] [sxcl-game] 崩溃注入:raise(SIGTERM) -> signal=15 code=143(可捕获)
    game-session: 运行器崩溃兜底回传信号:SIG signal=15 code=143
    game-session: 运行器已结束:信号=15 退出码=143 来源=socket pid-alive=1
    $ adb shell ps -A | grep sxcl
    u0_a170  17778  ... S com.silentstudio.sxcl          <- 只剩主进程

    ════ 3) --es gamecrash kill(SIGKILL,不可捕获)—— 如实报告,不编数字
    [game] [sxcl-game] 崩溃注入:kill(getpid(), SIGKILL) -> 不可捕获(对端只能看到通道断开)
    game-session: 通道已断开(EOF)
    game-session: 运行器进程不在了,但**没有退出记录**:被 kill -9 / OOM 杀时,退出码与信号
                  从进程外面不可观测 —— 如实报 killed,不编数字
    game-session STATE: ... game-pid=18110 state=killed connected=0 exit-code=? exit-signal=0
                  reason=channel-closed(pid 已消失) source=proc-poll
    $ adb shell ps -A | grep sxcl
    u0_a170  17778  ... S com.silentstudio.sxcl          <- 只剩主进程

    ════ 4) 主进程发 END(有序结束)
    game-session: 已发出结束命令(END):等运行器有序收尾(flush 日志 + 写退出记录)并确认它消失
    game-session: 运行器自报退出:EXIT code=0 signal=0 reason=command
    game-session: 运行器已结束:信号=0 退出码=0 来源=socket pid-alive=1
    game-session: 确认 :game 进程已消失 pid=18196(主进程照常活着 pid=17778)
    $ adb shell ps -A | grep sxcl
    u0_a170  17778  ... S com.silentstudio.sxcl          <- 只剩主进程

### 9.4c 最终 APK 上的画中画 / 回到启动器 / 重新启动

    ════ 两个进程 + 画中画(同一时刻)
    $ adb shell ps -A | grep sxcl
    u0_a170      18627  ... S com.silentstudio.sxcl
    u0_a170      18703  ... S com.silentstudio.sxcl:game
    $ adb shell "dumpsys activity activities | grep -E 'mode=pinned|mLastReportedPictureInPictureMode'"
      * Task{e91a1c1 #1355 ... mode=pinned translucent=false sz=1}
        mLastReportedMultiWindowMode=true mLastReportedPictureInPictureMode=true
    logcat: [sxcl-game] 启动器画中画握手:主进程回答 need=1
            pip mode changed: in-pip=true
            pip back-button shown(画中画里的出口;decor children=3)

    ════ 回到启动器(按钮做的那件事:exitFloating = reorder-to-front 意图)
    $ adb shell dumpsys window | grep mCurrentFocus
      mCurrentFocus=Window{36214 u0 com.silentstudio.sxcl/com.silentstudio.sxcl.SxclActivity}
    $ adb shell "dumpsys activity activities | grep -c mode=pinned"
      0
    $ adb shell pidof com.silentstudio.sxcl:game
      18827            <- 回到启动器之后游戏进程仍然活着

    ════ 上一次被拒的会话不会卡住(代码路径 + 真机形态)
    现在"发布新会话"时,如果上一个会话停在 `listening`(没等到运行器连接)—— 例如它那次启动
    被系统 BAL_BLOCK 掉 —— 会先释放它再开始。真机这一轮没有复现到那个触发条件(因为
    "画中画里起游戏"已经被下面的修复挡住),所以这条**只有代码路径与日志断言,没有真机原文**,
    如实记在这里。

### 9.4d 一个真机发现:启动器在前台时,游戏进程会被系统**冻结**

把启动器完全拉到前台(游戏退到后面)之后,游戏进程变成 cached,被系统的 cached app freezer 冻住:

    $ adb -s ... shell "dumpsys activity processes | grep -A3 '18827:com.silentstudio.sxcl:game'"
      Proc # 1: cch    b/ /LAST --------  t: 0 18827:com.silentstudio.sxcl:game/u0a170 (previous-expired)
      isFreezeExempt=false isPendingFreeze=false isFrozen=true

后果与取舍(必须写清,别当成 bug 悄悄绕过):
* 冻结期间通道上发过去的 `END` **不会立刻被处理**(它躺在 socket 缓冲里,等解冻才读到)——
  所以主进程那次会如实记一行
  `game-session: 8000ms 内没有确认 :game 消失 pid=18827(如实记录,不做任何强杀)`;
* 这**正是画中画这条路的必要性**:启动器在画中画里时,游戏仍是全屏栈顶的活动,
  **不会被 cached 冻结** —— 同一份 logcat 里游戏进程每 5 秒一次的 PING 心跳一直没断
  (`pings=60` 那行就是它);
* 产品化要补的一条:用户在启动器里点"结束游戏"而游戏已在后台被冻结时,界面应当
  先说清楚"游戏在后台已挂起,把它拉回前台后才会结束",或者先把游戏任务拉到前台再发 END。

### 9.5 两棵树整树构建 + ctest(门槛,安静窗口那一版)

运行窗口:2026-09-21 21:22:20 → 21:23:29。**跑之前那一刻的进程列表与产物写入时刻**(自证窗口):

    --- [before] running build/test processes (cl/MSBuild/link/ninja/cmake/ctest) ---
      MSBuild x7 pids=17256,18032,18740,21448,23292,24156,30688
    --- [before] newest write in each build dir ---
      ...\build-ui    newest=09/21/2026 21:16:09
      ...\build-norm  newest=09/21/2026 21:14:33
      ...\build       newest=09/21/2026 21:16:47
      ...\build-c     newest=09/17/2026 16:03:37
      D:\SilentStudio\PyQf to C\build  newest=09/18/2026 00:18:46

如实标注:**那一刻仍有 7 个 MSBuild 进程存在**(不是我的;我的 APK 构建 21:19 就结束了),
但它们在 21:16 之后**没有向任何产物目录写过东西**(见上面的 newest 时刻,已经静了 6 分钟)。
我用的是**自己的独立构建目录**(仓库根的 `build-round19` 与 `PyQf to C\build-round19`),
不与 build-ui 共享 `_sxcl_ui_resume_fixture` 之类的夹具;这次运行期间我动过的目录只有这两个
build-round19、以及 `D:\sxcl_local`(APK/日志)。**没有碰 build-ui、也没有碰它的夹具目录。**

    === tree1 SXCL C: build + ctest ===
      tree1 build rc=0
        100% tests passed out of 46
        Total Test time (real) =  38.57 sec
      tree1 ctest rc=0
    === tree2 PyQf to C: build + ctest ===
      tree2 build rc=0
        100% tests passed out of 3
        Total Test time (real) =   4.24 sec
      tree2 ctest rc=0

(tree2 的 ctest 必须把 `D:\Qt\6.11.2\msvc2022_64\bin` 放进 `PATH`,否则三个用例都是
`0xc0000135 STATUS_DLL_NOT_FOUND` —— 那是运行环境缺 DLL,不是代码问题。)

### 9.6 设备离开之前**没有验到**的(不许说成验过)

用户 2026-09-21 21:2x 把平板拿走了("平板我要拿走啊大哥"),因此下面这几条**只有代码与日志断言,
没有真机原文**:

1. **"在画中画里点启动游戏现在能成功(先回全屏再起)"** —— 代码已经这么做
   (`GameHost.requestStart: if (isInFloating()) { exitFloating(); +1.8s 后 startGameActivity }`),
   真机上没跑到那个分支:B 组测试时上一局的会话还活着,得到的是 `ERR:already-running`(正确行为),
   B2 组又遇上"启动器回前台后游戏进程被 cached 冻结"(§9.4d),会话没能结束,同样没进到那个分支。
2. **"上一次被拒的会话不再卡住"** —— 代码路径在 `requestStart`(发现 `listening` 且未连接就释放),
   真机这一轮没有复现出"被拒的启动"(BAL 那条已经被第 1 条的修复挡住了)。
3. **用手指点画中画里的"回到启动器"按钮** —— `adb shell input tap` 在这台 ROM 上打不进 pinned 窗口
   (§9.2 已如实记录),按钮做的那件事(exitFloating)用同一意图验证过。
4. 崩溃注入 `abort|term|kill` 三条**已验**(§9.4b 原文),但"注入发生在游戏窗口还没就绪时"这种
   边角时序没验。

设备上留下的东西(用户下次上机时可直接处理,都不是半成品):
* 我们自己的 APK(debug)已安装:`com.silentstudio.sxcl`;它可能还在跑(启动器 + :game 两个进程),
  直接划掉/强行停止即可;
* 应用私有目录里 5 个会话目录 `/data/user/0/com.silentstudio.sxcl/files/game/s*`
  (每个里面是 `game.log` / `exit.txt` / `host-state.txt`,验收留证用;卸载即清);
* **没有**往 `/storage/emulated/0` 写任何东西,没有碰 `/storage/emulated/0/FCL`,
  没有推送任何 JRE/临时文件,没有改任何系统设置,没有安装任何 MC 版本。
